# دراسة معمارية دمج محرك الويب (Chromium / CEF) مع فريم ورك ENKI

وثيقة مقارنة تفصيلية لمساعدتك على اتخاذ القرار الهندسي الأمثل لبناء **متصفح الويب الخاص بالنظام** أو تضمين صفحات الويب داخل واجهات **Enki** الأصلية.

---

## 1. نظرة عامة على المشكلة الهندسية

عند رغبتك في بناء متصفح ويب أو تطبيق يعتمد على ودجتس Enki في الواجهة (الألسنة، شريط العناوين، القوائم)، يحتاج محرك Chromium لرسم محتوى صفحات الويب وعرضه للمستخدم. 

التحدي الأساسي في أنظمة لينكس (X11 / Wayland) يكمن في: **كيف نربط سطح رندر Chromium بسطح رندر Enki (Skia / Vulkan / OpenGL) بأعلى أداء وبأقل استهلاك للموارد؟**

---

## 2. دراسة الخيارات المعمارية الثلاثة

### الخيار الأول: الرندر غير المرئي عبر المعالج (Classic Software OSR)
* **آلية العمل:**
  يطلب CEF من Chromium عمل رندر للصفحة، ثم يقوم المحرك بنسخ البكسلات عبر ممر PCIe من كارت الشاشة إلى الذاكرة العشوائية (RAM) عبر دالة:
  ```cpp
  void OnPaint(CefRefPtr<CefBrowser> browser,
               PaintElementType type,
               const RectList& dirtyRects,
               const void* buffer, int width, int height);
  ```
  بعد ذلك يقوم Enki برفع هذه البكسلات من الـ RAM مجدداً إلى كارت الشاشة لترسمها Skia كنسيج (`SkImage`).
* **المسار:**
  $$\text{GPU (Chromium)} \xrightarrow{\text{PCIe Readback}} \text{CPU RAM} \xrightarrow{\text{PCIe Upload}} \text{GPU (Enki / Skia)}$$
* **العيوب:**
  - استهلاك عالٍ جداً للـ CPU والـ Memory Bandwidth.
  - هبوط ملحوظ في معدل الإطارات (Drop Frames) عند تشغيل فيديوهات 4K أو ألعاب WebGL ثلاثية الأبعاد.

---

### الخيار الثاني: مشاركة النسيج عتادياً عبر DMA-BUF (Zero-Copy Texture Sharing)
* **آلية العمل:**
  يعتمد على تقنية **DMA-BUF** المدمجة في نواة لينكس (Kernel DRM/KMS). في هذا النمط، Chromium يقوم بالرندر داخل ذاكرة كارت الشاشة (VRAM)، ولا يقوم بنسخ أي شيء للـ CPU. بدلاً من ذلك، يمرر لـ Enki مقبض ملف عتادي (File Descriptor) عبر:
  ```cpp
  void OnAcceleratedPaint(CefRefPtr<CefBrowser> browser,
                          PaintElementType type,
                          const RectList& dirtyRects,
                          const CefAcceleratedPaintInfo& info);
  ```
  يستقبل Enki هذا الـ File Descriptor ويقوم باستيراده مباشرة داخل Skia / OpenGL / Vulkan عبر امتداد `EGL_EXT_image_dma_buf_import` أو Vulkan External Memory.
* **المسار:**
  $$\text{VRAM (Chromium Surface)} \xrightarrow{\text{Direct GPU Handle Sharing}} \text{VRAM (Enki SkImage / Texture)}$$
* **المميزات:**
  - **Zero-Copy بنسبة 100%:** لا يمر أي بكسل عبر المعالج أو ناقل PCIe.
  - صفحة الويب تصبح عبارة عن ودجت نيتف طبيعي في Enki؛ يمكنك وضع فلاتر بلور (Blur)، زوايا دائرية، أو تأثيرات حركية (Transitions) تعلوها أو تمر من خلفها.

---

### الخيار الثالث: التضمين العتادي المباشر كسطح فرعي (Native Subwindow / Subsurface)
* **آلية العمل:**
  لا يقوم Enki برسم صفحة الويب على الإطلاق! بدلاً من ذلك، نطلب من خادم العرض (X11 Server أو Wayland Compositor) إنشاء نافذة/سطح فرعي تملكه عملية Chromium وتكون تابعة جغرافياً لنافذة Enki الرئيسية:
  - **في بيئة X11:** ربط النافذة عبر `window_info.SetAsChild(enki_x11_window_id, rect)` أو `XReparentWindow`.
  - **في بيئة Wayland:** إنشاء سطح فرعي عبر بروتوكول `wl_subsurface`.
* **المسار:**
  خادم العرض أو مجمع الشاشات (Compositor) يدمج سطحين مستقلين مباشرة على الشاشة (Direct Scanout / Hardware Overlay).
* **المميزات:**
  - أداء فائق ومجاني برمجياً (Native Hardware Decoding & Video Acceleration VA-API).
  - استهلاك المعالج = 0% واستجابة فورية بدون أي كود استيراد معقد في الرسوميات.
* **العيوب:**
  - محدودية في رسم عناصر شفافة تعلو صفحة الويب مباشرة (يحتاج لإدارة Z-Order أو استخدام النوافذ المنبثقة Overlays).

---

## 3. جدول المقارنة الشامل لاتخاذ القرار

| معيار المقارنة | 1. المعالج البرمجي (Software OSR) | 2. مشاركة العتاد (DMA-BUF) | 3. النافذة الملحقة (Child Window / Subsurface) |
| :--- | :--- | :--- | :--- |
| **استهلاك المعالج (CPU)** | مرتفع جداً (ثقيل) | **صفر (Zero-Copy)** | **صفر (Zero-Copy)** |
| **استهلاك ناقل الذاكرة (PCIe)** | مرتفع جداً (ذهاباً وإياباً) | **صفر (محلي داخل VRAM)** | **صفر** |
| **فيديوهات 4K وألعاب WebGL** | بطيئة وتقطع | **سلسة جداً (60/120 FPS)** | **أعلى أداء عتادي ممكن (Native)** |
| **التأثيرات البصرية والبلور** | مدعومة | **مدعومة بالكامل مع كل شادرز Skia** | محدودة (السطح مغلق ومستقل) |
| **تعقيد الكود البرمجي** | سهل جداً | متوسط (يحتاج إعداد EGL/DMA-BUF) | سهل جداً (مدعوم أصلاً في نواة CEF) |
| **التوافق مع X11 و Wayland** | مدعوم على الاثنين | **مدعوم بقوة على الاثنين حديثاً** | مدعوم (عبر XReparent أو wl_subsurface) |
| **طبيعة المعروض في Enki** | بكسلات في الذاكرة | **كائن `SkImage` تفاعلي حقيقي** | نافذة نظام مستقلة مقيدة بالإحداثيات |

---

## 4. مسار القرار الموصى به (Recommended Roadmap)

### المرحلة الأولى (الآن / البداية السريعة):
> **اعتماد الخيار الثالث (Subwindow / Child Window):**
> يمنحك متصفحاً فائق السرعة بأقل وقت تطوير؛ حيث ترسم واجهة المتصفح العلوية والجانبية بـ ودجتس Enki، وتترك مساحة العرض الداخلية لـ CEF كنافذة فرعية عتادية. هذا يعطيك متصفحاً جاهزاً للعمل ومشاهدة الفيديوهات بكفاءة 100%.

### المرحلة الثانية (المستقبل / الدمج الكامل والتأثيرات الفاخرة):
> **الترقية إلى الخيار الثاني (DMA-BUF):**
> عند رغبتك في عمل ثيمات بصرية متقدمة لنظامك (مثل نوافذ الويب ذات الزوايا الدائرية الناعمة مع تأثيرات الزجاج والبلور المتحرك الممتزج مع خلفية النظام)، يتم تفعيل الـ DMA-BUF لتصبح مساحة الويب ودجت رسومي مدمج بالكامل في شجرة Enki.
