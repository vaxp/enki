# ============================================================
# ENKI Framework — Real Applications Directory
# ============================================================

if not is_android
  subdir('calculator')
endif
subdir('counter')
